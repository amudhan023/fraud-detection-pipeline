# PostgreSQL configuration for logical replication (required by Debezium)
# Must explicitly set listen_addresses because we override the default config file
listen_addresses = '*'
wal_level = logical
max_wal_senders = 4
max_replication_slots = 4
wal_keep_size = 1024
